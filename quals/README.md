# Quals

`npm run quals` runs two suites:

- Solver quals (Node): `quals/solver_quals.js`
- Browser/UI quals (Puppeteer): `quals/ui_quals.js`

To run just one:

- `npm run quals:solver`
- `npm run quals:ui`
